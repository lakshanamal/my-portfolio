import React from "react";

const projectPOp = ({ focus }) => {
  return (
    <div
      style={{
        position: "absolute",
        alignItems: "center",
        justifyContent: "space-around",
        width: "100%",
        height: "100%",
        alignSelf: "center",
        zIndex: 10,
      }}
    >
      <h3>Hello</h3>
    </div>
  );
};

export default projectPOp;
